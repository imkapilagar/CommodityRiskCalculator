# Premium Calculator Configuration
# This file contains manual overrides for index values when APIs fail

# Set Sensex manually if needed (leave empty to use API)
# Example: SENSEX_VALUE="81234.56"
SENSEX_VALUE=""

# Fallback values (used when API fails)
NIFTY_FALLBACK="24500.00"
SENSEX_FALLBACK="80500.00"
BANKNIFTY_FALLBACK="52000.00"
